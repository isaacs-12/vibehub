{
  "name": "vibehub",
  "version": "0.1.2",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/web",
    "packages/desktop"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspace=packages/web",
    "lint": "npm run lint --workspaces"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
