{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Main window: core defaults + dialog for Open Project folder picker",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
