import React from 'react';

/**
 * Segment layout for project pages (e.g. /acme/payments-service).
 * Passthrough — no UI.
 */
export default function RepoLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
